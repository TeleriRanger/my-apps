# Serve all routes to index.html for SPA, except for static assets
/todo/*    /todo/index.html   200

# Exclude static assets and API routes from the redirect rule
/todo/assets/*   /todo/assets/:splat  200
